{
  "name": "gymondo-workouts-app",
  "version": "1.0.0",
  "private": true,
  "description": "A simple app to list Workouts",
  "author": "Mohammad Saber",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:up": "cd apps/api && pnpm run db:up",
    "db:down": "cd apps/api && pnpm run db:down",
    "db:setup": "cd apps/api && pnpm run db:generate && pnpm run db:push && pnpm run db:seed",
    "db:studio": "cd apps/api && pnpm run db:studio"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "prettier": "^3.2.5",
    "turbo": "^2.5.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@8.15.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}